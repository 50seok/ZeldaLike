# Godot 4 standard
.godot/
project/.godot/
export.cfg
# export_presets.cfg: 1인 개발이라 민감정보 없음 - 버전관리 포함(M5 웹/Windows 설정 보존)

# Godot engine binaries (downloaded, not project source)
godot/

# 웹/exe 빌드 산출물(export_presets.cfg로 언제든 재생성 가능, 용량 커서 제외)
project/build/

# OS
Thumbs.db
.DS_Store
